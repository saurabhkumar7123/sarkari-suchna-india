node_modules/
.env
.env.local
.env.staging
.env.*.local
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
logs/
# Runtime JSON logs (never commit); keep data/ dir via .gitkeep
data/admin-activity.json
data/activity.json
data/related-analytics.json
!data/.gitkeep

# Package 4C editorial review file store (local human workflow state)
server/data/editorial-reviews.json
server/data/editorial-reviews.json.tmp
!server/data/.gitkeep

# Generator PDF extract temp uploads (multerPdfExtract)
storage/temp/

# Local backups and audit artifacts (Phase deploy prep — do not commit)
.backup-*/
audit-screenshots/
.deploy/

# Uploaded PDFs (runtime user content)
storage/uploads/

# Local OCR traineddata (runtime; do not commit large binaries)
eng.traineddata
hin.traineddata
*.traineddata

# Local one-shot M2/M3/M4 ops scripts (not production source)
scripts/_m2_*
scripts/_m3_*
scripts/_m4_*

# Enterprise / preview runtime JSON (local operator state)
server/data/enterprise/
server/data/shared-previews.json

tmp-*.png
scripts/tmp-*
.rc-archive/
docs/release-candidate/tmp-*
coverage/
