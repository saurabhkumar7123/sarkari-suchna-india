/**
 * /categories — SSR renders a standalone taxonomy list for ?tab= context.
 * Cross-taxonomy tabs are intentionally not used; homepage is the switcher.
 * Kept as a no-op shell so existing script includes remain harmless.
 */
(function initCategoriesStandalonePage() {
  "use strict";
  // Standalone page content is server-rendered; no client tab switching.
})();
